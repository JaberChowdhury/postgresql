info:
  name: Reset Database
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/seed
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
