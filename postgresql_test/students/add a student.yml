info:
  name: add a student
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/students
  body: json
  auth: inherit

body:json:
  {
    "name": "Jane Doe",
    "roll": "456"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
